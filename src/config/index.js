// Configuration values
const CONFIG = Object.freeze({
  // HD Path, prefixes, coin
  HD_PATH: "44'/5555'/0'/0/",
  HD_PATH_ARR: [44, 5555, 0, 0],
  BECH32_PREFIX: "und",
  BECH32_VAL_PREFIX: "undvaloper",
  BASENUMBER: Math.pow(10, 9),
})

export default CONFIG
